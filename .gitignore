node_modules/
# all runtime state under data/ — auth.json, audit.log, labels.json, devices.json,
# pairing/token stores, tmp/bak/corrupted variants — never publish to the public repo.
data/*
.env
public/vendor/
logs/
backups-*/

# editor / migration backups — never publish
*.bak
*.bak.*
.env.bak*
